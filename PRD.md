# PRD: QRコードによる参加ステータス自動更新機能

## 1. 目的・背景
**解決する課題:**
* **手動入力の非効率性:** 紙の名簿やスプレッドシートの目視確認による入場遅延。
* **データの不正確さ:** ヒューマンエラーによるチェック漏れや二重チェック。
* **リアルタイム性の欠如:** 運営が「今何人来ているか」を把握するのに集計の手間がかかる。

**プロジェクトの目標:**
* **受付の自動化:** QRコードスキャンからスプレッドシート反映までを自動化し、受付時間を大幅に短縮する。
* **データの即時同期:** GASを利用してスプレッドシートへリアルタイム書き込みを行い、管理コストをゼロにする。

## 2. ターゲットユーザー
* **イベント運営者:** スマホやPCのブラウザからスキャンを行い、受付業務を完結させたい。
* **イベント参加者:** ConnpassのQRコードを提示するだけでスムーズに入場したい。
* **管理者:** スプレッドシート上でリアルタイムの動員数を確認したい。

## 3. 機能・要件
### 3.1. 主要機能
* **スキャナーWebアプリ (GAS Web App):**
    * ブラウザ上でカメラを起動し、Connpass発行のQRコード（URL等）を読み取る機能。
* **スプレッドシート照合・更新機能:**
    * 読み取ったデータ（Connpass ID等）をキーに、スプレッドシート内の対象行を検索。
    * 「出席ステータス」列を「出席」に、現在時刻を「受付時間」列に自動入力。
* **重複チェック・アラート:**
    * 既に「出席」となっているIDを読み取った場合、「既に受付済みです」と警告。
* **エラーハンドリング:**
    * リストに存在しないIDの場合、「未登録のユーザーです」と表示。

### 3.2. UI/UX要求
* **フィードバック表示:**
    * **成功:** 画面全体が緑色になり「[名前]さん、ようこそ！」と表示。
    * **失敗/重複:** 画面全体が赤色になりエラー理由を表示。
* **音声フィードバック:** スキャン成功時に「ピッ」、エラー時に「ブッ」などの音を鳴らす（ブラウザ設定に依存）。
* **連続スキャンモード:** 1人スキャンするごとにリロードせず、連続して次々読み取れるインターフェース。

## 4. スコープ
### 開発の範囲 (In Scope)
* **GAS Webアプリケーションの開発:** スキャナー画面（HTML/JS）およびスプレッドシート操作ロジック（.gs）。
* **スプレッドシート連携:** 特定の列を「出席フラグ」として更新する仕組み。

### 除外するもの (Out of Scope)
* **データベース構築:** スプレッドシートをDBとして流用するため、外部DB（SQL等）の構築は行わない。
* **QRコード発行:** Connpass側の機能に依存。
* **オフライン対応:** GAS Web Appの性質上、常時オンライン（インターネット接続）を前提とする。
    * ※GASはサーバーサイド実行のため、完全なオフライン同期は今回の構成から除外します。

## 5. 指標
* **受付処理時間:** QR提示からスプレッドシート反映完了まで**3秒以内**を目指す。
* **操作ミス率:** 手動で「Ctrl+F」検索をしてチェックを入れる作業を**0回**にする。
* **同期速度:** スキャン完了から管理者が閲覧するシートへの反映ラグを**1秒以内**に抑える。

---

## 技術的な補足（GAS構成案）

1.  **スプレッドシート構成:** A列: Connpass ID / B列: 名前 / C列: 出席ステータス / D列: 受付時間
2.  **GAS (backend):** `doGet()` でスキャナー画面を返し、`google.script.run` を経由してシート更新関数を呼び出す。
3.  **Frontend (HTML/JS):** `jsQR` などのライブラリを使い、カメラ映像からQRコードを解析。
